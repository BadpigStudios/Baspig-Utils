package baspig.apis.utils.files;

/**
 * UNUSED
 */
public enum AssetType {
    /**
     * ASSETS type if for general assets folder
     * TEXTURE type if for texture folder into TEXTURES
     */
    ASSETS,
    TEXTURE,

    /**
     * DATA, type if for general data folder
     * RECIPE type if for recipe folder into DATA
     * TRIM_PATTERN type if for trim_pattern folder into DATA
     */
    DATA,
    RECIPE,
    TRIM_PATTERN

}
